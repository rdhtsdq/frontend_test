import { Layout } from "antd";
import Article from "../../domain/model/Article";
import Navbar from "../components/Navbar";

const ArticleDetail: React.FC<Article> = (props) => {
    return (
        <Layout>
            <Navbar />

        </Layout>
    )
}